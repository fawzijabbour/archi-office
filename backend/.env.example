DATABASE_URL=postgres://archi:archi_pass@localhost:5432/archi_office
JWT_SECRET=change_this_secret_in_production
PORT=4000
UPLOAD_DIR=./uploads

# Optional: set these to enable email delivery of notifications.
# Leave SMTP_HOST empty to keep notifications in-app only.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=ArchiOffice <no-reply@archioffice.local>
